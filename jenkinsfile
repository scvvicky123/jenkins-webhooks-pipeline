pipeline {
    agent any

    stages {
        stage('workspace') {
            steps {
                echo 'finding workspace'
                sh 'touch jenkinsscript.sh'
                sh './jenkinsscript.sh'
            }
        }
    }
}
