pipeline {
    agent any

    stages {
        stage('workspace') {
            steps {
                echo 'finding workspace'
                sh 'pwd'
                
            }
        }
    }
}
